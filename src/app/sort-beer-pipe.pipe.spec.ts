import { SortBeerPipePipe } from './sort-beer-pipe.pipe';

describe('SortBeerPipePipe', () => {
  it('create an instance', () => {
    const pipe = new SortBeerPipePipe();
    expect(pipe).toBeTruthy();
  });
});
