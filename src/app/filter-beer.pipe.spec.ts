import { FilterBeerPipe } from './filter-beer.pipe';

describe('FilterBeerPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterBeerPipe();
    expect(pipe).toBeTruthy();
  });
});
